# Rust
target/
src-tauri/target/
# Bundled native libs at the workspace root only (NOT src/lib, which is React).
/lib/
*.AppImage
Forge.AppDir/

# Models
*.gguf

# Node
node_modules/
dist/
.DS_Store

# IDE
.vscode/
.idea/

# Legacy GPUI layout, kept alongside during migration
src-old/
Cargo.toml.old
Cargo.lock.old
